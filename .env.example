PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=roomease_dev

JWT_SECRET=replace_with_a_long_random_string
JWT_EXPIRES_IN=7d

# Origin of the standalone React dev server (see /client), used for CORS.
CLIENT_ORIGIN=http://localhost:5173
