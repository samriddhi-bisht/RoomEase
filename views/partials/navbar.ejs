<header class="site-nav">
  <div class="nav-inner">
    <a href="/" class="brand"><span class="brand-mark">RE</span>RoomEase</a>
    <nav class="nav-links">
      <a href="/search">Find a room</a>
      <% if (user) { %>
        <% if (user.role === 'student') { %><a href="/roommates">Roommates</a><% } %>
        <% if (user.role === 'owner') { %><a href="/listings/new">List a place</a><% } %>
        <% if (user.role === 'admin') { %><a href="/admin">Admin</a><% } %>
        <a href="/kyc">KYC</a>
        <a href="/dashboard">Dashboard</a>
        <a href="#" id="logout-link">Log out</a>
      <% } else { %>
        <a href="/login">Log in</a>
        <a href="/signup" class="btn-pill small">Sign up</a>
      <% } %>
    </nav>
  </div>
</header>
